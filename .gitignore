# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Vercel — .vercel/output is the build artifact, .vercel/project.json is the
# local link to your Vercel project. Neither belongs in the repo.
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Convex snapshot exports — contain auth sessions/refresh tokens and user data.
# Keep database dumps out of the repo entirely.
backups/
*.zip

# Personal Claude Code permission grants — not shared config.
.claude/settings.local.json
